export async function POST() {
  return Response.json({
    ok: true,
    message:
      "Social retry placeholder is ready. Configure channel credentials and extend adapters for live posting approvals."
  });
}
